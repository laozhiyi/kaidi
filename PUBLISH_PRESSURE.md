# 多用户同时发单：本地并发验证

2026-09-14 补测。此前的“100 次相同发布”检查同一个请求的幂等性；本报告使用不同用户身份、不同请求编号和不同订单内容，同时启动各自的提交。

## 实测结果

以下为 `.tmp/publish-pressure.log` 中本轮独立运行的结果。客户端按实际代码执行最多 2 次自动补试，云端按实际代码执行最多 5 次事务尝试；没有为压测放宽重试次数。

| 场景 | 并发提交数 | 最终成功 | 数据库订单数 | 使用客户端重试后成功 |
| --- | ---: | ---: | ---: | ---: |
| 100 个不同用户，各发布一单 | 100 | 100 | 100 | 0 |
| 300 个不同用户，各发布一单 | 300 | 300 | 300 | 0 |
| 1000 个不同用户，各发布一单 | 1000 | 1000 | 1000 | 66 |
| 100 个身份，每个身份有 5 个独立客户端同时发布不同订单 | 500 | 500 | 500 | 1 |
| 200 个不同用户，每单 6 张图片，注入写入失败与丢回复 | 200 | 200 | 200 | 60 |

最后一组注入 20 次事务提交前写入失败，以及 50 次提交成功后的回复丢失。其中部分订单同时遇到两种故障，因此有 60 个订单需要客户端重试，共执行了 70 次客户端补试；最后仍只有 200 个订单。

1000 人一组发生了 2546 次模拟事务冲突，并观测到最多 1000 个在途事务。冲突经过云端退避和客户端重试处理。所有场景逐条核对了：

- 返回订单编号与实际文档一致，发布者、标题、取件信息、送达地址和图片数量对应各自输入。
- 没有已返回成功但未保存的订单，没有额外订单，没有用户内容串单。
- 每单只有一条发布事件、一条请求记录、一条发布通知，初始版本为 1。
- 每个用户的配额列表完整、不重复，与该用户实际订单完全一致。
- “可接单”接口通过游标读取后，能取到全部已保存订单且不重复。
- 自动补试始终复用原请求编号，成功后清理该客户端的待确认状态。

随机分片、随机退避与本机调度会改变每次运行的冲突数和重试数；上表记录这次运行的观测值。

## 验证范围

运行的是实际 `operations_biz.js` 客户端提交逻辑、订单控制器参数校验、订单服务、配额与事务重试代码。模拟器让事务真实重叠，以文档版本变化判定提交冲突，并加入固定 10ms 的人工提交延迟。

身份、数据库 I/O、微信传输、内容审核与图片存储均为本地替身，没有连接线上云环境。图片场景验证的是六张图片的业务处理、归档引用及重试完整性，没有上传真实图片或测量微信图片审核吞吐量。

因此，此结果是业务并发正确性验证，**不代表已经通过真实云环境的 1000 人容量压测**。不能从本地用时推算腾讯云 QPS、延迟或手机帧率。真实容量仍需要在独立测试云环境检查函数并发、数据库额度、实时监听连接数、网络、审核接口和后台任务积压。

## 复现

```powershell
node --test scripts/tests/publish-pressure.test.js
npm test --prefix cloudfunctions/mcloud
```

测试文件：`scripts/tests/publish-pressure.test.js`；场景与核对逻辑：`scripts/test-support/publish-pressure.cjs`；乐观事务模拟器：`scripts/test-support/concurrent-transactions.cjs`。这些文件仅用于本地测试，不部署到云函数。
