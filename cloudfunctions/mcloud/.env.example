# Reference only. The application does not load this file automatically.
# Set values in the cloud function environment settings. Never commit secrets.
CLOUD_ENV_ID=
INIT_ADMIN_NAME=admin
# Required ONLY for a new database. Set a unique strong value in the console.
INIT_ADMIN_PASSWORD=
# Production: false (or omit). Never set true in production.
CONTENT_CHECK_DISABLED=false
# Optional: absent template means in-app messages only.
ORDER_SUBSCRIBE_TEMPLATE_ID=
# Replace mapping with your actually approved template fields, as JSON.
ORDER_SUBSCRIBE_FIELDS={}
