# 云开发稳定性与流畅度上线说明

本次修改覆盖小程序公共请求、列表与导航层，并加强订单、反馈、评价和客服消息等写入流程。代码和本地验证已完成，云函数、权限、索引和小程序仍需按本文配套发布。本地模拟的并发测试不能代替腾讯云环境的容量测试。

## 已实现的行为

- 发布、接单、取件、送达、取消、确认收货等订单变更在云数据库事务中执行。订单状态、版本、履约历史、请求记录、用户订单配额、通知与刷新信号一起提交；失败整体回滚。
- 多人同时抢同一单，仅一个人可接取。其他人收到订单已变化的提示并重新读取列表。取消与接单、编辑与接单也由云端事务裁决。前端禁用按钮只是改善交互，最终结果由云端决定。
- 手机网络超时后使用同一个请求编号重试，不重新创建一份订单。确定的事务冲突最多尝试 5 次并带随机退避；客户端最多自动补试 2 次。未知提交结果不由云端事务包装器盲目重放。
- 提交结果不明且想修改内容时，可点击“核对上次提交”。云端返回已保存的记录；若尚未保存，在事务中停止旧请求，使延迟到达的旧请求不能再写入。核对成功后才解除该请求的本地占用。已保存订单不会被这个操作取消。发布页重新打开后仍有核对入口，当前表单不会被核对操作清空。
- 订单公共列表、个人订单列表和详情共用实时监听。新订单或状态变更提交后，通过最多 64 个分片信号触发刷新。突发信号合并处理，列表更新最小间隔 1.5 秒；刷新过程中有新信号会补刷。
- 实时监听正常时约 30 秒兜底刷新；监听失败后从约 8 秒起轮询，失败时退避并尝试重连，均有随机抖动。网络恢复会补刷。页面离开、程序进入后台后停止相关监听与计时器。
- 切换订单分段先展示同账号、同筛选条件下 60 秒内的内存快照，再读取最新结果。多页列表在深处阅读时出现更新入口，回顶部自动更新，避免强行跳回第一页。
- 公共请求合并相同的在途读取，按身份隔离，并给每个调用方独立数据。公共列表忽略旧响应、合并分页时去重，刷新失败保留内容。订单使用稳定排序和游标翻页，降低并发新增或移除导致的漏项。
- 常用按钮立即显示选中或处理中状态；移除接单成功后的人工等待。读取提示不遮挡整个界面。启用组件按需加载，减少无变化的 `setData`。上传失败保留原图和已上传结果，重试不会静默丢掉图片。
- 客服发送及后台回复使用稳定请求编号和事务去重。收藏量批量聚合查询，50 个订单的独立收藏状态读取为 3 次数据库查询。维护任务单条失败继续处理其他条目，并覆盖已取件订单的超时提醒。

“立即响应”指交互反馈及时；订单成功、列表最新结果仍需要云端和网络确认。旧的进行中订单不会因为接单截止时间已过而从个人列表消失。

## 数据库权限

在微信开发者工具的云开发控制台逐集合配置自定义安全规则。规则用于客户端访问；服务端云函数使用自身权限访问集合。

仅 `bx_order_feed` 使用 [order-feed.json](deployment/database-rules/order-feed.json)：

```json
{
  "read": "auth != null && doc._pid == 'crun'",
  "write": false
}
```

这里的登录指微信云开发身份，不表示通过业务注册审核。该集合只有 `_pid`、`shard`、`revision`、`updatedAt` 等刷新信号，不含订单编号、地址、手机号、取件码或收藏者身份。客户端查询固定带 `_pid: 'crun'`；若修改项目标识，须同时修改规则、服务端和监听查询。

其余业务集合使用 [server-only.json](deployment/database-rules/server-only.json)：

```json
{
  "read": false,
  "write": false
}
```

包括 `bx_mail`、`bx_user`、`bx_admin`、`bx_fav`、`bx_order_quota`、`bx_order_event`、`bx_order_request`、`bx_notification`、`bx_news_read`、`bx_subscription`、`bx_feedback`、`bx_feedback_request`、`bx_order_review`、`bx_review_request`、`bx_identity_unique`、`bx_operation_config`、`bx_operation_audit`、`bx_operation_limit`、客服消息及其他业务集合。公开页面的数据也由云函数校验、过滤后返回；不要为了实时刷新开放整个订单集合。

控制台若要求表达式字符串，可将上述布尔 `false` 录入为表达式 `false`。保存后用普通小程序身份验证：只能订阅本项目的 `bx_order_feed`，不能直接读取私密集合或写入任何业务集合。未配置监听权限时会退到轮询，无法获得最佳实时体验。

云存储权限与数据库规则分开设置：保留当前用户向 `private/<openid>/` 上传的权限，禁止客户端写入 `private-evidence/`。审核后的凭证由云函数归档；私密图片由云函数核实订单关系后生成临时访问地址。逐件包裹图片也经过相同处理。不要把数据库规则 JSON 应用成整个存储桶的上传策略。

## 复合索引清单

下表是当前查询的基础配置；全部为普通非唯一索引。按字段顺序创建，`↑` 为升序，`↓` 为降序。已有完全相同的索引直接复用。`_id` 自带唯一索引，但不替代含 `_id` 的复合排序索引。

| 集合 | 用途 | 字段顺序 |
| --- | --- | --- |
| `bx_order_feed` | 本项目监听 | `_pid ↑, shard ↑` |
| `bx_mail` | 可接单、首页最新订单 | `_pid ↑, MAIL_STATUS ↑, MAIL_PAYMENT_MODE ↑, MAIL_ADD_TIME ↓, _id ↓, MAIL_END_TIME ↑` |
| `bx_mail` | 可接单高价优先 | `_pid ↑, MAIL_STATUS ↑, MAIL_PAYMENT_MODE ↑, MAIL_OBJ.price ↓, _id ↓, MAIL_END_TIME ↑` |
| `bx_mail` | 我的发布 | `_pid ↑, MAIL_USER_ID ↑, MAIL_ADD_TIME ↓, _id ↓` |
| `bx_mail` | 我的接单 | `_pid ↑, MAIL_ACCEPT_USER_ID ↑, MAIL_ADD_TIME ↓, _id ↓` |
| `bx_mail` | 发布配额初始化 | `_pid ↑, MAIL_USER_ID ↑, MAIL_STATUS ↑` |
| `bx_mail` | 接单配额初始化 | `_pid ↑, MAIL_ACCEPT_USER_ID ↑, MAIL_STATUS ↑` |
| `bx_mail` | 待接单自动过期 | `_pid ↑, MAIL_STATUS ↑, MAIL_PAYMENT_MODE ↑, MAIL_END_TIME ↑` |
| `bx_mail` | 履约超时提醒 | `_pid ↑, MAIL_STATUS ↑, MAIL_DUE_TIME ↑, MAIL_OVERDUE_NOTIFIED ↑` |
| `bx_mail` | 后台订单列表 | `_pid ↑, MAIL_ADD_TIME ↓, _id ↓` |
| `bx_mail` | 后台按状态筛选 | `_pid ↑, MAIL_STATUS ↑, MAIL_ADD_TIME ↓, _id ↓` |
| `bx_fav` | 单批订单收藏计数 | `_pid ↑, FAV_OID ↑, FAV_TYPE ↑` |
| `bx_fav` | 本人收藏状态 | `_pid ↑, FAV_USER_ID ↑, FAV_OID ↑, FAV_TYPE ↑` |
| `bx_fav` | 我的收藏列表 | `_pid ↑, FAV_USER_ID ↑, FAV_ADD_TIME ↓, _id ↓` |
| `bx_notification` | 订阅消息派发 | `_pid ↑, delivery ↑, nextAttemptAt ↑` |
| `bx_notification` | 本人消息列表 | `_pid ↑, userId ↑, createdAt ↓, _id ↓` |
| `bx_notification` | 本人未读消息 | `_pid ↑, userId ↑, read ↑, createdAt ↓, _id ↓` |
| `bx_operation_limit` | 过期限流桶清理 | `_pid ↑, expiresAt ↑` |
| `bx_user` | 注册身份读取 | `_pid ↑, USER_MINI_OPENID ↑` |
| `bx_campus_service_message` | 用户会话与翻页 | `_pid ↑, CSM_SERVICE_ID ↑, CSM_USER_ID ↑, CSM_ADD_TIME ↓, CSM_ID ↓` |
| `bx_campus_service_message` | 管理员会话与翻页 | `_pid ↑, CSM_SESSION_ID ↑, CSM_ADD_TIME ↓, CSM_ID ↓` |
| `bx_order_review` | 订单是否已评价 | `_pid ↑, REVIEW_FROM_USER_ID ↑, REVIEW_ORDER_ID ↑` |

公告和评价/申诉列表的其余索引沿用 [NOTIFICATIONS.md](NOTIFICATIONS.md) 与 [FEEDBACK_VALIDATION.md](FEEDBACK_VALIDATION.md)。

上述清单不穷举所有筛选组合。校区、加急、已完成、归档、后台价格排序与时间筛选需要在测试环境逐项执行，按云数据库的索引提示及查询分析补齐。等值筛选字段放在排序字段之前；范围筛选与 `OR` 条件要用实际执行计划确认。送达地点包含搜索和普通关键词搜索使用正则，普通复合索引不能让所有包含搜索都变成索引点查；数据量增长后需基于测量选择专门搜索方案。

## 发布顺序

1. 在测试云环境验证，备份现有数据库或开启云端备份。保留所有历史订单、配额、请求及审计记录。
2. 部署整个 `cloudfunctions/mcloud` 并在云端安装锁定依赖。不要只上传前端。客服发送现在需要 `requestId`；用户端与管理员端均须更新。生产切换应安排短暂维护窗口，并等待旧版在途云函数结束后再启用新版客户端的核对功能，避免旧实例不识别停止标记。
3. 首次调用会检查并补齐必需集合，包括 `bx_order_feed`。`bx_setup_crun_20260914` 是本版初始化标记，只有所有必需集合就绪后才创建；不要手动提前创建该标记。后续冷启动仅检查标记，正常热调用复用初始化结果。
4. 为新集合配置上述安全规则和索引，确认索引建立完成。若人为删除了必需集合，须恢复集合并重新核对初始化状态，不能只留下版本标记。
5. 同步云函数 `config.json`，确认 `operations-minute` 定时触发器为每分钟执行；保留原有 `ORDER_SUBSCRIBE_TEMPLATE_ID`、`ORDER_SUBSCRIBE_FIELDS` 和 OpenAPI 权限。云端控制台的触发器需实际存在，单独上传代码未必同步触发器。
6. 根据云套餐配置函数并发、内存、超时时间、数据库读写额度与连接数量。客户端请求超时默认 20 秒；函数超时、审核/图片耗时应在测试环境评估，不用调大前端超时掩盖长期拥塞。
7. 上传并发布对应小程序。使用两个微信账号检查自动刷新、抢单与隐私权限，再分批放量。生产用户更新前要明确处理旧版客服接口的兼容期；旧客户端缺少请求编号时会收到校验错误，不能静默降级为无幂等写入。

不要删除 `bx_order_request`、`bx_feedback_request` 或 `bx_review_request` 来“清除重试”，其中也保存核对后停止旧提交的标记。删除会让延迟请求重新生效。定时清理只删除过期限流桶，不删除业务记录和审计。

## 真机与真实云容量验收

至少使用两个微信账号，覆盖 iOS / Android、弱网、断网再恢复、前后台切换。检查：

1. A 发布，B 停留在可接单页自动出现订单；B 接单，其他人的列表更新。高速连续发布时仍能看到最后一次变化，后台停止轮询。
2. 多账号同单抢单只产生一位接单人、一份接单事件、正确配额；失败者提示明确。取消与抢单同时操作后状态一致。
3. 分别模拟提交前断网、提交后丢回复。恢复后相同请求不重复保存；点击“核对上次提交”后，旧请求不能在新请求之后意外写入。核对已成功的订单不改变其状态。
4. 反复切换筛选、翻页和分段，旧响应不覆盖新列表。长列表阅读位置保持；回顶部展示更新。所有取件、送达、提交按钮第一次点击即有处理反馈。
5. 纯文字与六张图片、多人聊天、收藏计数与后台操作分别压测。普通身份不能直接读取其他人的联系方式、取件信息或请求记录。

在隔离测试环境按 20、50、100、200 活跃客户端递增负载，分别记录成功率、P50/P95/P99、事务冲突、超时、数据库读写量、实时连接数和审核额度。每轮独立统计成功业务数、实际记录数、事件、配额与通知，一单多抢属于预期业务拒绝，应与服务故障分开统计。先明确目标峰值和可接受延迟，再以实测值配置容量。

当前维护任务每次分别扫描最多 30 条过期订单、30 条超时订单、30 条待派发通知及 100 条限流桶，内部最多 4 个并发工作者。站内通知与订单在事务中即时保存；微信订阅派发异步执行。必须监控最早待派发时间、积压数量和维护失败数；持续流量超过处理能力时，在压测后增加有时间预算的批量派发或独立工作函数。不能把定时器正常运行当作队列没有积压。

数据库服务故障、权限错误、网络中断或超出购买容量时仍可能失败，但界面应保留可重试状态，不显示虚假的“保存成功”。云开发本身也需要权限、备份、告警和容量配置，不能保证任意并发、任何故障下都零丢失。

## 本地检查

2026-09-14 最终本地验收：

| 检查 | 结果 |
| --- | --- |
| 自动化回归 | 284 项通过，0 项失败 |
| 页面、组件、导入与静态路由 | 63 个页面、114 个 WXML 模板通过 |
| 页面状态渲染 | 99 种状态通过，含提交结果核对入口 |
| 微信 WXML/WXS 编译 | 115 个源文件通过 |
| 微信 WXSS 编译 | 129 个源文件通过 |

本地日志：`.tmp/reliability-final-tests.log`、`.tmp/reliability-final-compile.log`。未执行线上部署、真实云并发压测或真机帧率测量。

```powershell
npm ci --prefix cloudfunctions/mcloud --ignore-scripts --no-audit --no-fund
npm test --prefix cloudfunctions/mcloud
node scripts/check-miniprogram-pages.cjs --wcc '微信开发者工具路径\wcc.exe' --wcsc '微信开发者工具路径\wcsc.exe'
```

回归包含真实重叠的乐观事务模拟、100 人争抢同一单、100 次相同发布、提交结果核对竞争、重复客户端请求、断网/重连、分页、旧响应、图片失败、客服丢回复与后台操作。云数据库和微信传输使用本地替身；微信编译器检查模板和样式。这些验证不等同于线上并发认证或真机帧率测试。

后续补测了 100、300、1000 个不同用户同时发布不同订单、500 单跨客户端配额竞争，以及 200 单附图故障重试；结果与测试边界见 [多人同时发单验证](PUBLISH_PRESSURE.md)。
