# 云开发稳定性与流畅度上线说明

完整部署顺序、迁移、权限、索引和真实云验收以 [部署手册](deployment/MULTI_TENANT_ROLLOUT.md) 为准。旧版仅按项目隔离和客户端数据库监听的部署方式已停止使用。

## 已实现的行为

- 发布、接单、取件、送达、取消、确认收货等变更在云数据库事务中执行。订单版本、事件、请求记录、配额、站内通知与刷新信号一起提交，失败回滚。
- 抢同一单由云端事务决定唯一接单人；接单/取消、接单/编辑冲突同样由事务裁决。前端禁用按钮只提供交互反馈。
- 同一请求编号重试不会重新创建订单；确定的事务冲突最多尝试 5 次并带随机退避。客户端最多自动补试 2 次。未知提交结果不由云端事务包装器盲目重放。
- “核对上次提交”返回已经保存的结果，或在事务中停止尚未保存的旧请求。请求归属保护保留原学校/校区，切换校区不能绕过去重或停止标记。
- 每个请求由服务端解析稳定的学校/校区 ID。数据库访问统一加范围，跨校区文档、聚合和更新同样受限；缺失范围不能退化为全表查询。学校共享账号使用学校范围。
- 前端读取合并、缓存、快照、未读与配置按范围区分；校区切换后丢弃旧响应，切换学校清除业务登录。正在写入时阻止切换，未知提交保留原请求以便核对。
- 订单页面共享一个云函数刷新信号查询，每 10–12 秒获取当前校区最多 64 条不透明版本；变化合并刷新，最短刷新间隔 1.5 秒。另有约 30 秒兜底，失败退避及网络恢复补刷。页面和小程序隐藏时停止。
- 公告使用校区摘要及用户已读版本索引；摘要正常命中时不遍历全部公告与阅读历史。通知每 60 秒、收藏每 30 秒同步，主动操作完成后及时刷新。
- 列表使用稳定排序与游标，静默刷新避免重复总数统计。60 秒内的内存快照按身份、学校、校区、筛选条件隔离。长列表有更新入口，不强制跳回顶部。
- 取/送/买插件提供校验、计价和表单差异；订单引擎保留授权、事务、幂等、配额及状态机。订单保存业务版本和规则快照，营业时间由服务端按 UTC+8 校验。
- 订单维护与微信订阅派发是两个独立定时函数；按时间和文档 ID 续扫，有批量、时间预算、失败计数和持久检查点。某条异常记录不会一直挡住后面的任务。

“立即响应”指按钮与页面反馈及时，订单成功及数据最新状态仍需要云端确认。范围过滤防止返回另一校区业务数据，不是对网络、外部通知投递或任意容量的保证。

## 数据库与存储权限

所有集合均使用 [server-only.json](deployment/database-rules/server-only.json)，包括原来用于监听的 `bx_order_feed`：

```json
{"read": false, "write": false}
```

[order-feed.json](deployment/database-rules/order-feed.json) 也已关闭读写。客户端通过 `operations/feed` 获取当前校区的版本摘要，不直接 get/query/watch 数据库。安全规则必须在控制台实际应用；所有普通身份的数据库直读/直写均应失败，合法业务仍通过云函数运行。

云存储权限单独配置：仅允许用户写自己的上传目录，禁止客户端写私密凭证归档目录。临时私密链接由云函数检查订单关系后发放，不要把上述数据库规则作为存储桶上传策略。

## 复合索引

使用 [候选索引清单](deployment/database-indexes.json)。校区查询以 `_pid, schoolId, campusId` 开头，学校账号以 `_pid, schoolId` 开头；只有受控目录、全局身份、迁移和定时扫描使用全局索引。时间游标同时包含 `_id`。

```powershell
node scripts/check-architecture-deployment.cjs
```

展开后的字段顺序写入 `.tmp/architecture-validation/indexes-expanded.json`，供人工核对。清单未应用到云端，不能代表查询已经命中索引。按真实数据逐项检查列表排序、状态、加急、时间、地点、后台导出及聚合执行计划。正则包含搜索、OR 与不等于条件需单独评估扫描量，不盲目增加全部字段组合。

## 发布与运行

完整操作执行 [多校部署手册](deployment/MULTI_TENANT_ROLLOUT.md)，尤其注意：

1. 明确独立测试环境 ID，备份数据并演练恢复。显式设置 `CLOUD_ENV_ID`，不要默认使用仓库中的原环境配置。
2. 暂停旧写流量和旧定时任务，按冻结计划完成全量 plan/apply/verify 和逐校区 finalize。保持历史 ID、请求和审计记录，不清库。
3. 配套部署 API、小程序、权限和索引。新初始化标记是 `bx_setup_crun_20260921_tenants`；不要手工提前创建。
4. 删除 API 上旧的 `operations-minute` 触发器。新 `ordersWorker/orders-minute` 与 `notificationsWorker/notifications-minute` 分开部署，API 不接收 Timer。
5. 验证真实运行时、微信订阅模板/权限、冷启动、数据库与审核配额。校区默认暂停，完成配置和验收后再逐步开放。
6. 关闭/删除临时迁移函数，保留备份与日志，并监控错误、冲突、任务积压和资源用量。

不可删除 `bx_order_request`、`bx_feedback_request`、`bx_review_request`、`bx_request_scope` 来“清除重试”。其中保存停止旧提交与请求归属信息。定时自动清理仅处理过期限流桶。

## 核验边界

本地回归覆盖真实重叠的乐观事务模拟、多账号争抢、相同请求竞争、提交核对、弱网重试、分页、旧响应、图片失败、客服、租户隔离、迁移与后台授权。实际安装 SDK 的底层请求被拦截验证，没有连接真实数据库。微信编译器验证模板与样式。

```powershell
node --test --test-reporter=tap --test-timeout=10000 scripts/tests/*.test.js
node scripts/check-operations-sdk.cjs
node scripts/check-miniprogram-pages.cjs --wcc '<微信开发者工具内的 wcc.exe>' --wcsc '<微信开发者工具内的 wcsc.exe>'
```

每次修改后重新执行上述检查，以当前源码的命令结果为准。

真实环境仍需核验数据库规则、索引计划、运行时兼容、通知到达、事务一致性、P95/P99 延迟与配额。将预期业务拒绝（例如抢单失败）与服务错误分开计数；逐级放量并观察最早待处理时间、积压与失败记录。

## 发布与申诉并发核验

```powershell
node --test scripts/tests/publish-pressure.test.js scripts/tests/feedback-concurrency.test.js scripts/tests/feedback-network-retry.test.js
```

发布测试运行实际客户端重试、控制器、订单服务与配额逻辑，使用按文档版本检查冲突的并行事务模拟器；覆盖不同用户同时发单、同一请求重试、图片引用、提交前失败和提交后丢回复，逐条核对订单、事件、请求、通知及配额，不以本地吞吐推算云端容量。

申诉由 `feedback/insert` 写入，后台通过 `admin/feedback_list`、`admin/feedback_detail` 和 `admin/feedback_reply` 处理，用户在“我的申诉”查看；回复同时生成站内通知。相同请求重试复用原记录，不重复审核、归档和扣提交次数；管理员并发回复按版本校验，失败保留草稿。每用户每小时最多新提交 5 条、每分钟最多发起 10 次新提交审核。

申诉与评价索引使用 [索引清单](deployment/database-indexes.json) 中带学校、校区前缀的定义；新增筛选组合依真实查询计划补充。独立测试云环境逐级做 20／50／100 并发的纯文字和六图提交，核对成功响应、实际记录数、管理员可见数量、回复结果、超时率和 P95 延迟。

管理员软删除订单后，收藏把它标记为失效，仍可取消已有收藏；订单维护跳过该订单，不再产生过期或超时事件。旧事件和通知保留，禁止把审计或幂等记录当缓存清理。
