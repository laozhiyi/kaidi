# 公告与站内消息

## 用户使用方式

- 首页展示已发布公告的标题、摘要和未读标记，点击直接阅读；“全部”进入公告列表。优先展示未读的重要公告，其次是其他未读公告。后台将公告排序设为 `0` 即标记为重要。
- 首页“消息”和“我的 → 消息中心”显示未读数量，超过 99 条显示 `99+`；底部“我的”显示红点。数量包含公告、订单动态和申诉等个人通知。
- 消息中心按时间展示公告与个人通知，支持“只看未读”“全部已读”和分页。公告、订单、申诉、信誉分通知直接打开相应详情，普通通知在弹窗中显示完整内容。
- 进入消息列表不会清除未读。详情成功加载并展示后才记录已读；详情失败、跳转失败不会误清除。已读记录保存在云端，按用户隔离，更换设备后仍生效。
- 前台共用一个 25 秒刷新计时器，返回小程序时立即刷新；退到后台后停止轮询。消息中心发现变化时展示“消息有更新，点击刷新”。网络异常保留最后确认的未读数量，恢复后再同步。

公告微信推送未纳入本次范围。现有订单微信订阅及其模板配置继续保留，消息中心仍提供“订阅下一次订单通知”。

## 后台发布与数据

新建公告先保存为未发布草稿，封面、正文和表单图片上传成功后才发布。同一页面上传失败后重试复用草稿，避免重复创建公告；返回后台列表后，也可继续编辑未发布记录。发布接口使用实际数据库文档 ID，确保图片和正文写到同一篇公告。

公告直接读取现有 `bx_news` 集合，不向每个用户复制一份广播。新增 `bx_news_read` 记录 `{ _pid, userId, newsId, readAt }`，文档 ID 根据项目、用户、公告生成，重复阅读不会重复增加记录。个人通知继续使用 `bx_notification`。

只有 `NEWS_STATUS = 1` 的公告展示并计入未读。停用或删除后，它会在下一次刷新时从用户列表与未读数量中移除。编辑或重新启用同一篇公告保留原有已读状态；需要重新提醒所有用户时，发布一篇新公告。升级前已发布的公告在没有阅读记录时计为未读，用户可以逐条阅读或选择全部已读。

“全部已读”先取得当前消息 ID，再写入阅读状态；写入期间到达的新消息仍保留未读。个人通知的阅读更新不会覆盖微信投递状态。

## 部署

1. 部署 `cloudfunctions/mcloud` 云函数并安装依赖。
2. 上传并发布对应的小程序代码。用户端和云函数需配套更新。
3. 云函数初始化会自动补齐 `bx_news_read`，无需重置用户、公告、通知或订单订阅数据。已读操作均通过云函数完成。
4. 在云数据库控制台按查询提示补齐复合索引。常用查询涉及以下字段：

| 集合 | 查询场景 | 索引字段顺序 |
| --- | --- | --- |
| `bx_news` | 首页公告 | `_pid` 升序、`NEWS_STATUS` 升序、`NEWS_ORDER` 升序、`NEWS_ADD_TIME` 降序、`_id` 降序 |
| `bx_news` | 未读统计扫描 | `_pid` 升序、`NEWS_STATUS` 升序、`_id` 升序 |
| `bx_news_read` | 用户阅读记录 | `_pid` 升序、`userId` 升序、`_id` 升序；按公告筛选时增加 `newsId` |
| `bx_notification` | 用户消息列表 | `_pid` 升序、`userId` 升序、`createdAt` 降序、`_id` 降序；只看未读时增加 `read` |

部署后可用两个测试账号检查：发布一篇公告，确认两人均看到未读；其中一人打开详情，只清除自己的未读；停用公告后两人的列表和计数均更新。再检查订单订阅入口，以及订单和申诉通知能否直达详情。

未读统计按每批 100 条读取公告及阅读记录的必要字段，避免云 SDK 的默认返回数量截断计数。公告历史量很大时应结合云函数监控评估查询耗时。

## 本地验证

```powershell
node --test --test-reporter=spec scripts/tests/*.test.js
npm ci --prefix cloudfunctions/mcloud --ignore-scripts --no-audit --no-fund
node scripts/check-operations-sdk.cjs
node scripts/check-miniprogram-pages.cjs --wcc 'B:/school/微信web开发者工具/resources/app.asar.unpacked/node_modules/wcc-exec/wcc.exe' --wcsc 'B:/school/微信web开发者工具/resources/app.asar.unpacked/node_modules/wcc-exec/wcsc.exe'
```

编译器路径需根据本机安装位置调整。测试覆盖未读持久化、用户隔离、超过 100 条阅读记录、分页同时间戳、批量已读期间的新消息、退到后台、过期响应、网络失败、阅读时机及发布失败重试。

本地示例页面可用以下命令生成，使用实际 WXML/WXSS 和示例数据，不访问云端：

```powershell
node scripts/build-admin-layout-preview.cjs --fixtures=scripts/test-support/notification-layout-fixtures.cjs --output=.tmp/notifications-preview.html
```

预览包含首页、消息中心、个人中心、公告列表和错误状态。模板编译与示例渲染不等同于微信真机验收；本次未部署云端或发送真实通知。

2026-09-14 清理旧页面后的本地验证：242 项测试通过，63 个注册页面与 113 个云端路由核对通过，98 个页面状态渲染通过，115 个 WXML/WXS 源文件及 129 个 WXSS 源文件编译通过。真实 SDK 检查因本机缺少依赖、npm 下载被环境网络权限拒绝而未完成。浏览器预览检查被自动审批服务拦截（审批接口返回 404，不支持所选审批模型），尚未完成视觉验收。
