# 公告与站内消息

云端部署、数据库权限／索引及迁移统一执行 [部署手册](deployment/MULTI_TENANT_ROLLOUT.md)，功能入口与本地检查见 [README](README.md)。

## 用户行为

- 首页展示当前校区已发布公告的标题、摘要和未读标记，点击阅读；“全部”进入公告列表。优先显示重要且未读公告，后台排序为 0 表示重要。
- 首页、个人中心与底部红点共用未读数量，包含本校区公告、订单和申诉等个人通知。消息中心支持只看未读、全部已读、游标分页和详情入口。
- 详情成功加载展示后才记录已读，页面跳转/读取失败不会误清除。阅读记录按学校、校区和用户隔离，同一校区跨设备保留。
- 前台共用一个 60 秒刷新计时器，返回小程序时刷新，退到后台停止。网络异常保留最后确认的数字；消息中心发现变化时显示更新入口。
- 切换校区后清理旧摘要，过期响应不能回填到新校区。批量已读捕获当前消息 ID，操作期间新增的消息仍保留未读。

公告微信推送不在本轮范围，现有订单订阅入口保留。

## 公告存储与摘要

公告正文保留在 `bx_news`；`bx_news_read` 保存逐用户阅读记录，`bx_notification` 保存个人通知。新增校区级 `bx_news_manifest` 保存有效公告摘要/版本，`bx_news_unread` 保存用户该版本的已读 ID 索引，正常刷新不全量扫描历史正文。

新公告先保存草稿，正文和图片成功后发布。公告与摘要在一个事务中更新；有效公告最多 500 条、摘要最多 600,000 UTF-8 字节，超过边界需撤下旧公告。编辑同一篇公告保留已读，需要重新提醒应新建公告。停用/删除会在下次摘要刷新时移除。

摘要版本变化后，只查询当前有效公告的已读状态并合并同时到达的阅读确认。历史升级须完成迁移后的逐校区 finalize，重建早期可能为空的摘要；不要手工向正文集合写入后遗漏摘要更新。

“全部已读”按捕获的 ID 分批更新，个人通知阅读不会覆盖微信投递状态。操作部分失败可重试，不能将部分批次成功显示成全部成功。

## 微信订单订阅

站内通知与订单事务一起保存；微信订阅在 `notificationsWorker` 中异步领取、发送及记录结果，不占用 API 的同步请求链路。配置 `ORDER_SUBSCRIBE_TEMPLATE_ID`、`ORDER_SUBSCRIBE_FIELDS` 和 `subscribeMessage.send` 权限，字段必须对应实际模板。

新订阅链接携带学校、校区及通知编号，详情页先校验目录并切换到原校区，再读取订单。离开页面后晚到的目录响应不再切换校区。已发出的旧链接无法补写范围，使用旧链接时须先选择订单原校区。

发送失败遵守有限重试；不具备用户授权或模板条件时跳过并保留站内通知。发送成功但回执丢失可能造成再次发送，外部服务投递无法承诺严格一次。监控发送失败、正在发送的租约和最早待派发时间，而不只检查定时器是否运行。

## 权限、索引与部署

所有相关集合均关闭客户端数据库读写，包括刷新信号集合。合法读写由云函数校验身份和校区范围后执行。使用 [索引候选清单](deployment/database-indexes.json)，公告/个人通知/阅读记录索引加 `_pid, schoolId, campusId` 前缀；通知 worker 的全局队列扫描使用单独索引。

升级必须同时部署小程序、API 和独立定时函数，并完成旧数据迁移。旧版只部署 `mcloud` 与 `bx_news_read` 的步骤不适用于多校版本。移除旧 `operations-minute`，实际建立新的通知定时触发器。

## 核验

```powershell
node --test --test-reporter=tap --test-timeout=10000 scripts/tests/*.test.js
node scripts/check-operations-sdk.cjs
node scripts/check-miniprogram-pages.cjs --wcc '<微信开发者工具内的 wcc.exe>' --wcsc '<微信开发者工具内的 wcsc.exe>'
```

本地覆盖已读持久化、用户／校区隔离、大于单页的历史阅读、同时间戳分页、批量已读期间新消息、后台停止、旧响应、网络失败、阅读时机与公告发布重试。详情刷新失败应保留已经显示的内容并允许重试；订阅请求在后台完成后，重新进入页面仍可再次订阅。

真实云端至少以两校、三个校区和多个账号验证：公告互不串区、单人阅读只影响本人、停用后的计数变化、校区切换、订单与申诉详情入口、订阅实际到达及数据库直读被拒绝。模板编译与本地替身不等同于真机或微信投递验收。
