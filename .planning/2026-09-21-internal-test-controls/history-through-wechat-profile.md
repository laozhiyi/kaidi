# 进行中：微信头像昵称填写与独立保存（2026-09-23）

用户要求实时核验当前 AppID 的权限，可用时读取头像昵称。沿用 AppID `wx3d8dc6fb0e764ec7` 和已授权的官方开发者工具调用；已完成的旧账号清理不再执行，保留用户测试登录生成的新账号。

- [x] 20. 取得微信官方当前规则、AppID 元数据及项目运行时证据。
- [x] 21. 核对头像／昵称控件和隐私设置，区分自动读取、用户选择、模拟器能力与后台资格。
- [ ] 22. 按用户确认实现登录后头像昵称填写和独立保存，保留联系资料补全入口。
- [ ] 23. 验证真实页面表单、头像上传、保存失败／退出竞态与后端资料边界，交付代码改动。

用户在确认获取方式限制后明确要求改代码。本轮为已有登录与资料流程的局部改动：使用原生头像选择和微信昵称填写，登录后可只保存这两项；联系电话和学校资料继续用于发布／接单前补全。服务端仅更新当前可信微信身份的头像与昵称，不创建账号、不自动完成审核或联系资料。沿用当前工作区39份已有改动，不重新清理用户数据。

实时证据：开发者工具返回 AppID `wx3d8dc6fb0e764ec7`、名称“广校件跑”、envVersion=develop、SDKVersion=3.17.1；chooseAvatar 和 input.type.nickname 均为 true，getPrivacySetting 返回 needAuthorization=false。此为开发者工具结果，未获得真机头像昵称。公众平台网页停留登录页；用户已要求停止扩大权限调查，不再继续后台登录。

# 已完成：移除手动注册并删除已有普通用户账号（2026-09-23）

用户反馈点击微信登录仍显示旧流程／没有读取，要求直接注释手动注册选项，并对已有数据库用户做账号注销。

- [x] 16. 移除手动注册按钮，核对基础与旧手机号模式均不显示该选项。
- [x] 17. 移除手动注册方法及原生能力失败后的自动回退；服务端资料保存不得直接创建账号，只允许微信登录后补全。
- [x] 18. 核对云环境并删除全部旧账号：7 条账号资料、8 条手机号绑定删除成功；复查绑定为 0，用户表仅有用户确认在清理后测试登录生成的 1 条新账号。
- [x] 19. 完成本地验证、独立审查及云端删除复查；本任务未部署云函数或上传小程序。

用户已回复“就是字面意思，删除所有”，明确要求删除已有全部普通用户账号；随后说明“没有读取”是指退出手填账号后点微信登录，又恢复了同一旧账号，并再次要求删除手动注册。无需再次确认删除意愿，仍须查清目标环境和数据范围。原手填与微信登录均使用可信 OPENID，因此恢复旧账号符合旧实现；本轮改为只允许微信建号，并执行单独的旧账号清理。

手动注册按钮、方法、状态、样式和失败后的自动回退已移除；资料提交前及服务端事务内均要求账号存在，旧接口不能直接建号。624/624 全量、133/133 定向回归、209 个实际页面状态、119 个 WXML/WXS、131 个 WXSS 和 7 个组件样式检查通过；源码检查及 diff 检查通过。独立审查未发现可行动问题，另有 28 项回归和 12 项独立探测通过。云端旧账号及手机号绑定已按用户授权删除；本任务未部署云函数或上传小程序。

云端操作进展：用户已明确允许沙箱外调用；官方安装检查通过（2.02.2608060）。客户端授权成功，状态 `versionRelation=equal`、`loginExpired=false`、`tokenRequired=false`。`cloud_env_list` 返回 AppID `wx3d8dc6fb0e764ec7` 的唯一环境 `cloud1-d7g4yk2vu877ecc87`，与当前小程序配置一致，作为本次账号清理目标。

授权任务 `auth_4f3b13ec98c45266d9157f22cff6dd34cb7ab71bc45a523d` 已查询确认为 `success`，无待处理授权任务。

原用户删除任务 `confirmation_cloud_db_write_doc_c00aa0d4-70fc-4284-a02a-2f94f2a5517e` 已确认 `success`，云端返回 `deleted=7`、`requestId=e0955b7a-45c4-4ef3-9373-c68fddf37758`；勿重发。

绑定删除任务 `confirmation_cloud_db_write_doc_a5c5f4b6-da71-4ea3-86f2-ec7ae2e67767` 已确认 success，`deleted=8`、`requestId=12eb40f7-28b2-45f7-b35c-1c013e3ce77d`，无待处理任务。复查 `bx_identity_unique.total=0`，`bx_user.total=1`；该条创建时间 `1790129120002` 晚于两次删除完成时间，资料未完善。用户已确认正在测试微信登录，保留此新账号，旧账号清理完成。

# 已完成：基础微信登录与明确重新登录（2026-09-23）

用户已确认并要求开始修改以下两条：
1. 退出后保持未登录；打开登录页只显示登录界面，再次点击“微信登录”才恢复账号。已有账号和资料保留。
2. 保留手机号识别代码，隐藏相关入口并停用调用；使用云开发可信微信身份登录，不以微信手机号授权／验证为前置条件。

沿用现有工作区并保留起点20份既有改动。新用户可建立微信身份账号，发布／接单前仍需完成原有联系资料及审核；登录不得伪造手机号验证、自动通过审核或改变学校数据范围。

- [x] 12. 核对已确认需求、登录页自动恢复根因、前后端手机号门槛及现有测试。
- [x] 13. 保存当前基线，补充明确登录、新账号、旧账号保留、禁用手机号及竞态回归，观察预期失败。
- [x] 14. 实现基础微信登录及退出后明确点击流程，隐藏／停用手机号能力，同步资料与业务判断。
- [x] 15. 运行定向与全量回归、页面编译和变更审查，更新部署说明并交付。

## 本轮决策与边界

- 普通静默登录只恢复已有账号，不因页面加载创建新账号；创建账号由明确点击微信登录触发。
- 原手机号实现作为关闭的能力保留，未启用时客户端不调用、服务端拒绝兑换；基本登录不依赖该服务。
- 已验证手机号保持原值时保留验证标记；基础模式手动更改联系电话时必须清除验证标记并继续检查号码归属。
- 本地检查与真实云端／真机验收分开报告。本轮不擅自上传、提交或更改真实数据。
- 初次批量阅读输出过长，后续保存并分段读取；根目录无 package.json，使用 cloudfunctions/mcloud 的 npm test。

---

# 历史：订单真实进度与全功能排查

当前续作（2026-09-22）：修复订单详情顶部不真实的进度，检查各功能，清理无用文档并修复已证实问题。

当前续作（2026-09-23）：在个人资料页增加退出登录，确认后清理本地会话并返回“我的”；阻止静默恢复，保留主动重新登录流程。

## 范围与既有状态

保留当前工作区全部既有改动、手填注册、已取消的后台学校／校区授权限制、认证及数据范围。此前常用地址、审核、SDK 封装修复已完成本地核验；原计划和本轮起点 733 个文件已保存在 `.tmp/function-audit-before.zip`（附 SHA-256 清单）。本轮不把本地测试称作云端或真机验收。

## 阶段

- [x] 1. 根因调查：核对订单状态、履约事件、详情渲染及刷新；建立各功能检查清单。
- [x] 2. 行为复现：先运行当前基线，再为可确认的问题补回归并观察失败。
- [x] 3. 定点修复：真实订单进度及检查中发现的业务／交互错误；独立复核问题全部关闭。
- [x] 4. 文档清理：删除 5 份过时重复文档，保留 7 份维护文档，34 个本地链接有效。
- [x] 5. 验证：592 项全量、源码／路由、200 页面状态、WXML/WXSS 和变更复核通过。
- [x] 6. 交付：文件、文档链接、备份与本地部署包核对完成；明确尚未云端部署或真机验收。
- [x] 7. 上传提示续查：核实 63 份裁剪文件的可达依赖，以及截图中两个未使用组件的注册来源。
- [x] 8. 清理已确认的无用注册并检查页面编译、依赖与增量；说明重新扫描上传步骤及本地验证边界。
- [x] 9. 退出登录调查：确认资料页、静默登录、个人资料缓存与主动登录路径。
- [x] 10. 增加退出按钮和会话处理，验证取消、退出保持、旧请求及重新登录。
- [x] 11. 运行相关回归、页面编译与增量复核，记录本地验证结果。

## 验收结果

- 根因是遗漏发布、重复配送和累计点亮；管理员可跳过履约而直接完结。进度现按时间／动作证据显示，跨角色保留安全布尔标记，支持 ISO 历史日期，拒绝零值、负数和非法日期。15 项回归及独立三服务/四角色复核通过。
- 已修复删除订单仍能收藏／触发维护通知、详情弱网清空内容和草稿、隐藏页面订阅锁。两个审查者交叉复核无新发现。
- 真实 WXML 复现代买重复购买要求及隐藏订单信息，已修复；200 页面状态、119 WXML/WXS、131 WXSS 与组件样式检查通过。
- 最终全量 592/592、363 JS/CJS、103 JSON、943 导入、126 路由与 diff --check 通过。新包 architecture-B8Pp0p 三包各 122 文件校验通过，uploaded=false。733 份起点备份哈希全部一致。
- 工具记录：rg 路径用目录+glob；长输出保留日志。Node 子进程启动 rg 被拒绝，已改由 PowerShell 直接列文件后交给 Node 核对。
- 上传续查：63 份未打包文件均无入口依赖；清除两处页面三条无用组件注册。44/44 相关测试、200 页面状态、119 WXML/WXS、131 WXSS、源码与 diff 检查通过；上传与开发者工具重新扫描由用户执行。
- 退出登录：新增按钮、确认及持久退出状态，保留主动恢复既有账号；14项新增行为回归先失败后通过。最终全量606/606、203页面状态、119 WXML/WXS、131 WXSS、源码及diff检查通过；仅小程序端改动，无云端部署或真机验收。
