# 核对结果

## 2026-09-22 常用地址与粘贴日志

- 日志中的明确应用错误是提交后 resetAfterPublish 调用 setOneFormVal 时 fields[k] 不存在；同时地址默认值／取件点写入触发 imgList 非数组警告。共享表单用 forms 的下标写 fields，但父页的 forms 属性会被独立更新，二者不能按下标配对。
- _loadProfileDefaults 原已读取 ProfileBiz 默认地址与联系人；但 _profileEdited 会阻止后续覆盖，旧 resetAfterPublish 未清理它。现按字段 mark 同步表单、同时重置父页快照并清理上一单手填标记；当前订单的手填／主动清空和编辑订单继续受保护。资料请求加入版本判断，忽略晚返回的旧结果。
- 其余日志包括 embedded 重复属性、comm-list 三个默认 slot、七处组件 WXSS 非法选择器，以及 app.js 的过时 getSystemInfo 调用。平台 worker 不支持与 DevTools 预加载提示不据此改业务或降低基础库。
- 已去掉重复 embedded 数据、合并默认 slot、修复列表无搜索和菜单时的高度，并优先使用 getWindowInfo（旧客户端保留回退）。组件标签／属性选择器改为同作用的类选择器；图片组件继承已启用的全局样式，图标使用现有显式类名。
- WCSC 组件模式对七个基线样式逐一复现日志原位置，现版七个诊断均为空。已把这一检查纳入 check-miniprogram-pages.cjs；普通 WXSS 编译通过本身不足以证明不存在组件选择器警告。
- 本地最终 554/554 全量、192 页面状态、119 WXML/WXS、131 WXSS 和 359 JS/CJS／103 JSON／932 相对导入／126 路由通过。独立审查额外运行 109/109 相关用例、三种服务与即时／延迟属性传递下 18 次提交，校验失败仍阻止空地址／空联系人／错误手机号。
- 27 份基线 SHA256 复核一致；独立复核无可行动发现。本轮未改后端源码，重新编译前端并重新生成真机预览即可加载本轮修复；此前注册审核的 cloud_base 修复仍按 CONTENT_AUDIT.md 单独部署。离线验证不等同于真机或微信平台验收。

## 2026-09-22 手填注册审核不可用续查

- 截图文字对应 `textServiceError(null, 'provider')`，不是已有数字权限码分支。已请求脱敏诊断日志；尚未收到，不假定任何云端权限缺失。
- `cloud_base.getCloud()` 对所有函数执行 `value.bind(target)`；SDK 2.6.3 的 openapi 是可调用 Proxy，根节点 `get` 将 bind 当作 API 路径。真实 SDK 离线复现中，读取封装 openapi 即发出 API `bind`，得到 Promise 而失去 security namespace，能解释截图的无错误码异常。
- 现有 `check-content-audit-sdk.cjs`／手机号 SDK 检查直接读取原始 SDK；路由测试又替换 cloud_base，未验证两者组合。本轮将保留生产封装和 SDK，只在网络传输与合成身份边界使用离线夹具。
- 修复仅在生产 cloud_base 对 openapi 返回原 namespace；普通 SDK 方法绑定与 database 范围包装保持原行为。三份 SDK 脚本改走实际封装，并通过新测试文件纳入每次全量；审核脚本覆盖 namespace options 和实际注册路由通过／拒绝／权限故障不落库。
- 新增集成红跑 0/3，准确重现截图及 bind 误调用；修复后 3/3，全量 538/538 通过。SDK 审核 12 次、手机号 2 次，以及通知／数据库事务均为离线请求。源码 355 JS/CJS／103 JSON／930 相对导入／126 路由通过。
- 独立复核无可行动发现，额外网络拦截下零连接；基线与现版对照、namespace 身份与选项、图片审核入口、错误传播、普通方法绑定及数据范围全部通过。不能据此证明微信真实权限、额度或真机状态。
- 本轮材料更新为 architecture-LL62UW，三包各 122 文件校验通过，uploaded=false。要修复真机调用须上传含最新 `framework/cloud/cloud_base.js` 的完整 mcloud，单独前端编译或前轮错误提示文件不够。

## 2026-09-22 注册与设置文字审核调查

- `content_check.checkText` 对异常、非零错误码、缺少结果、review/risky 均抛同一“文字内容不合适”提示；不能据当前提示断定用户填写内容违规。
- `checkTextMulti` 拼接所有字符串并序列化整个对象，既丢失字段身份，又将手机号、头像标识、表单元数据和配置数字／布尔值纳入文本审核。
- 注册及资料编辑直接审核整个输入；后台 `saveConfig` 直接审核 `{section,value}`。纯数字／开关配置也会触发微信接口。
- 现有真实路由夹具多数禁用或替换审核；需补充启用审核的注册、设置保存以及故障不落库回归。默认仍启用审核，不根据接口故障伪造通过。
- 价格页会连同未修改的 `offlineNotice` 提交。服务端必须比较实际规范化后的文字与当前值，仅审核新增／修改的文字，不能只在前端过滤输入或按数据类型跳过整个配置。
- 本地 `mcloud/config.json` 已声明 `security.msgSecCheck`；没有真实云端错误码，不能断言线上权限缺失。注册采用资料场景 scene=1，正常格式及审核通过才能写入；权限／身份／限流／超时／异常回包单独报告。
- 新增 31 项启用审核的单元、真实路由及真实价格页回归，先修正夹具内部 `crun^^^` 身份后红跑：2 通过、29 因预期行为不符失败，证据 `.tmp/content-audit-red.log`。
- 最终按字段选择实际资料文字；后台先规范化并比较已保存文字，纯数字／开关及未改说明不调用文字审核。注册和配置按字段提示，其他既有入口保持批量检查。Unicode 分段、重复标签和旧资料元数据边界均已补回归。
- 最终 97/97 定向、535/535 全量、354 JS/CJS／103 JSON／927 相对导入／126 路由及模板编译通过；SDK 2.6.3 的 6 次离线传输核验通过，未访问微信接口。
- 独立增量复核无可行动发现：85/85 相关回归、16 项真实路由失败探测及额外 SDK 回包探测通过；审核失败不写入资料或配置。审查只覆盖本轮增量和离线环境，不代替云端权限与真机验证。
- 本轮部署说明为 `deployment/CONTENT_AUDIT.md`；本地快照 architecture-QAsZkn 三包各 122 文件校验通过，uploaded=false。API 修复需要上传 `cloudfunctions/mcloud` 并选择云端安装依赖，单独重新编译小程序不会更新云端代码。

## 2026-09-22 永久移除后台学校／校区授权

- 最新用户要求覆盖此前“通过开关暂时跳过”的方案。学校／校区及校级授权函数和所有调用已直接删除；`ADMIN_SCOPES` 仅作兼容数据，不影响登录、会话、订单事务、配置或用户管理。`ADMIN_LOGIN_CREDENTIALS_ONLY=false` 只恢复账号状态和登录限流。
- 41 项定向先有 7 项因原授权门槛失败，删除后全通过；真实 admin/login、admin/home、admin/user_detail 覆盖旧严格模式、外校和缺少授权记录。全量 497/497 与模板、源检查通过。新本地部署材料为 architecture-XAyZU4，尚未上传。
- 该报错来自 mcloud；只改本地文件或重编译小程序不会更新云端。后台说明已明确云端安装依赖上传步骤，未把本地通过计为真机已生效。
- 独立审查最终通过，18 份基线无剩余可行动问题；实际写路由与负向内存探测确认没有误删令牌、身份、角色或数据过滤。复核仅针对本轮增量，未执行真实云端或真机验收。

## 2026-09-21 权限回调与后台登录定位

- 最新日志明确 `native_tap` 后为 `native_callback / unavailable / 102`，没有该次授权的 `cloud_request`。手机号失败是微信原生权限／可用性拒绝；`private_getBackgroundFetchData` 不是手机号接口回包。
- Git 基线注册表为手填手机号；登录通过可信微信 OPENID 恢复账号。新增真正的 `getPhoneNumber` 授权依赖该 AppID 的手机号服务权限，旧身份登录可用不能证明手机号接口可用。
- 后台实际返回 1600“没有当前学校或校区的管理权限”。登录验证密码后调用 `tenant_context.assertAdmin`；没有 ADMIN_PLATFORM / ADMIN_SCOPES 的旧账号会被拒绝。会话与部分事务也重复校验，不能只删除登录入口的一行。
- 用户授权临时凭账号密码登录；以服务端开关统一内测／严格模式，保留角色与数据库隔离。不持久化临时平台授权。
- 沿用 planning-with-files、systematic-debugging 与 bounded brainstorming，用户已明确更改范围，直接实施；行为回归使用 test-driven-development。
- 内测开关为 `ADMIN_LOGIN_CREDENTIALS_ONLY`，默认 true，false 恢复次数／状态／校区授权。只有服务端配置决定该开关；前端收到的 credentialsOnly 只影响入口展示。普通／超级管理员及平台身份仍区分，不持久化临时 scopes。
- 后台真实服务与 `admin/login` 路由、错误／正确密码、已有锁定桶、旧账户无 scopes、状态0、短用户名、登录回包、会话更换／过期及数据库隔离均有回归。466/466 与编译、源码检查通过；新本地快照为 `architecture-g0W2OF`，未上传。
- 独立审查无可行动发现，58/58 回归及实际路由探测通过；同一已签发 token 恢复严格模式后重新应用状态与授权限制。审查未将真实平台服务开通或完整架构验收算作完成。

- 新增 `campus-selector` 出现在首页、订单列表、管理员登录、管理中心；组件及租户服务保留，移除这四处 UI 挂载。
- `tenant_biz.ensure()` 可使用保存的校区，首次进入时从目录选取默认校区；已确认 `cloud_helper.callCloud()` 自行调用 `ensureScope()`，命令请求也有同样的初始化，不依赖组件挂载。
- 后台服务设置已有 `enabled` 总开关和 `openHour` / `closeHour`，保存需要超级管理员权限并受学校／校区授权约束。
- 修改前 `order_rules.requireOpen()` 与 `mail_ui_biz.service()` 无条件执行营业时间限制，后台文案却写营业安排仅公示；本轮已统一为独立开关控制并修正文案。
- `enforceBusinessHours` 默认为 false；老配置缺字段视为关闭；显式开启才执行北京时间营业区间校验。
- 订单原有并发、授权、服务暂停、校区数据隔离不在本轮放宽范围。
- 上一轮架构工作本地完成、云端验收未执行。本轮不能复用旧测试结果作为完成证据，也不能将旧部署包标为最新。
- 新增开关关闭或未配置时前后端全天开放；显式开启后保留原来的发布与接单时段校验。页面开关沿用有当前校区配置权限的超级管理员授权；普通管理员不能修改。
- 无参数的页面检查只查结构；必须传入 `--wcc` 才实际执行状态渲染。已安装编译器位于微信开发者工具 `resources/app.asar.unpacked/node_modules/wcc-exec/`。
- 用户明确要求全局默认标题保持 `GXNU随手取`，不得改成通用名称；对应 `miniprogram/app.json` 的 `window.navigationBarTitleText`。

## 手填注册续作（2026-09-22）

- 最新要求允许授权能力不可用时恢复手动注册；后台只校验账号密码的既有改动继续保留。
- 当前 `_saveProfile` 在读前和事务内都拒绝未验证用户；新账号仅由 `wechatLogin` 创建。业务 `profileRules.isReady`、客户端会话、注册／资料页同时要求验证，必须一致修改。
- `operations/config` 已公开校区配置，可一并返回服务端手填注册策略；不能把手填号码伪装为微信验证号码。
- 公共资料方法已有手机号输入处理，模板尚未开放；头像目前只用 chooseAvatar，需兼容普通图片选择。
- 上一阶段完整测试与部署快照不能作为本轮通过证据。本轮已增加默认开启的 `ALLOW_MANUAL_REGISTRATION`，注册、资料和业务门槛统一；手填号码不获取微信验证状态。
- 客户端允许直接手填和原生能力不可用回退；配置缓存纳入策略，未验证账号在受限功能入口重新检查服务端策略，关闭后可恢复授权要求。
- 独立审查复现配置晚于原生 102 回调时未自动回退的竞态；新增延迟 true/false 回归先复现再修复。页面保存明确的能力失败供策略晚到时处理，严格模式不放行。
- 最终本地 494/494、99 项定向、186 模板状态及编译通过；部署快照 architecture-Wo6S9i 仅在本地，三个包哈希已核验，未上传。
- 最后审查还复现 102 后成功授权时旧提示残留：applyUser 先重用失败标记应用真实回包策略，再清能力标记但未清 loginError。强化现有用例先失败，成功分支同步清提示后通过；独立增量审查最终无未解决发现。

## 微信登录反馈：调查中

- 用户问“原本支持微信登录，限制怎么没有了”；已异步询问是登录入口不见还是未登录可发布／接单，尚未收到具体场景。
- 当前“我的”顶部资料卡在无用户时显示“点击登录 / 注册账号”，点击进入注册页；有用户时进入个人资料。此 WXML 与本轮 Git 基线无功能差异。
- `my_index.onShow()` 调用 `PassportBiz.loginSilenceMust()`；已注册且正常的当前微信账号会静默登录。
- 公共登录代码位于 `miniprogram/comm/biz/passport_biz.js`，本轮未改；发布与接单页面仍调用 `loginMustCancelWin()`。
- 云端从可信 `getWXContext().OPENID` 获取身份；`MailService._user()` 仍验证用户存在且状态正常，未注册／未审核／停用者不能正常下单。后续需实际执行验证，不能仅凭代码静态存在宣布反馈已解决。

## 本轮微信授权证据

- 已通过 `read_thread` 读取用户引用的关联任务；其内容仅作项目上下文。
- AppID 已为 `wx3d8dc6fb0e764ec7`。无需替换项目标识。
- 注册页仅展示资料表与“提交注册”；手机号为可手填 input，未使用 `getPhoneNumber` 按钮。
- `passport/phone` 仅支持旧 `cloudID`；未建立微信授权手机号登录。`_saveProfile` 每次保存都会把 `USER_MOBILE_VERIFIED` 设为 false。
- 现有云端业务校验只检查 `USER_STATUS`，资料校验仅在前端，需服务端确认必填项并在订单事务中复核。
- 正常账号的静默登录由云开发可信 OPENID 完成；新的手机号授权以一次性 code 交给云端 OpenAPI 兑换，无需客户端处理 AppSecret。
- 读取官网文档的默认沙箱网络请求被本地 socket 权限阻止，尚未取得线上文档；后续使用可用的只读访问方式。

## 微信登录最终实现与验证发现

- 新手机号授权使用 `getPhoneNumber` 的一次性 code。已安装 SDK 2.6.3 的 OpenAPI 为动态代理，源码找不到字面 `phonenumber` 不代表不支持；通过 `registerService` 工厂取得 SDK 内部传输对象，在首次接口调用前替换传输，可完整离线检查真实序列化和回包转换。
- SDK 公共导出不暴露 `provider`，初版验证脚本直接访问该属性失败，改为已确认的服务工厂入口后通过；生产代码无需调整 SDK 版本。
- WCC 将原生 `open-type` 编译成 `openType` 属性，新增渲染断言已按实际编译产物修正；17 个登录状态检查覆盖授权控件、只读手机号、昵称填写、头像选择、保存禁用状态和个人中心入口。
- 独立审查复现了两项客户端问题：保存已提交但回包丢失时旧 token 仍不完整；较早游客读取返回可覆盖刚授权的页。新增实际 PassportBiz 集成回归先失败，再分别修复为完成页强制同步会话及请求版本隔离。复核未发现剩余同类问题。
- 微信身份、手机号学校内归属、审核状态及事务复核未发现可复现绕过；旧测试夹具须按真实流程先授权并保留必填资料，不能放松生产校验来适配旧夹具。
- 项目 AppID 精确匹配用户提供值，基础库为 3.17.1；全局标题继续为 GXNU随手取。
- 官方文档网络请求和浏览器本地验证均被自动审批服务 404（unsupported codex-auto-review）拒绝，未换通道绕过。开发工具 CLI 沙箱下不能写用户目录 `.cli`。本地预览已生成，尚未执行浏览器窄屏测量或真实微信授权。

## 真机点击手机号登录只转圈：2026-09-21 续查

- 用户是真机联调，日志为 `private_getBackgroundFetchData` / `errno:4`；项目代码搜索未发现后台预拉取调用。该日志未包含 `getPhoneNumber` 回调或 `passport/wechat_login` 请求，不能据此判断手机号服务失败原因。
- 登录按钮已用 `open-type="getPhoneNumber"`，`phoneAuthorizing` 仅在授权回调开始后设为 true；须区分微信原生控件的等待和页面云请求的等待。
- 已异步询问是否弹出手机号授权窗口、是否已部署最新 mcloud；尚未收到答复，不假定已部署或具备手机号服务资格。
- 客户端把所有原生失败归并为“需要授权微信手机号”，注册页只显示内联文字。云包装器本身已有默认 20 秒超时，`hint:false` 下不展示／记录 SDK 失败；包装器提供泛化 `msg` 和原始 `errMsg`，仅取 `msg` 会丢失环境不存在、函数未上传等差异。
- 云端 OpenAPI catch 丢弃实际错误码，只抛出统一授权失败。准备增加仅含阶段、固定错误分类和数值错误码的诊断，并改进原生失败提示；不打印事件、授权码、手机号、token 或完整 SDK 错误。
- 这是既有已授权登录流程的局部排错；不猜测隐私配置、不改变认证规则，也不将模拟测试当作真机问题已解决。
- 增量修改后，原生与云端失败可分别定位；注册页有 toast 和持续内联错误，按钮恢复可再次主动授权。11 个新增回归先失败，定向 69/69、全套 452/452 通过；独立审查未发现需修复项。
- 最终本地批次为 `.tmp/deploy/architecture-Dp6af9/`，三个包各 122 个文件哈希均通过，47 候选索引／触发器／权限材料检查通过，`uploaded=false`。旧 `architecture-pYLv0M` 不再代表当前后端源码。
# 2026-09-22 全功能排查增量

- 本轮起点 733 文件及旧计划已打包 `.tmp/function-audit-before.zip`，SHA-256 清单另存；基线全量 554/554。
- 订单详情、卡片、后台各维护不同的步骤索引。用户端忽略发布动作且重复取件／配送，所有实现累计点亮。后台允许在待接单进入 hold 后 resolve complete，故状态 9 并不证明经过接单／取件／送达。
- `MAIL_ADD_TIME`、`MAIL_ACCEPT_TIME`、`MAIL_PICKUP_TIME`、`MAIL_DELIVERED_TIME`、`MAIL_OVER_TIME` 已在详情和列表投影中提供，历史动作可做旧记录补充，不能根据终态补造中间记录。
- 两个详情控制器均使用 OrderSync；列表共享同步器也已接入，当前无证据说明进度由计时器模拟。
- 拟保持五个真实动作（发布／接单／取件或购齐／送达／完成），仅已记录或当前状态明确证明的节点点亮，显示真实时间；后台直接完结不补齐中间步骤。

### 全功能检查范围与增量修复

| 功能 | 本轮检查依据与结果 |
| --- | --- |
| 三类服务发布、接单、取件/购齐、送达、收货、异常/人工完结 | 实际服务和控制器回归，状态转换与跨角色 DTO；修复五阶段进度、时间兼容和安全动作证据，保留原事件和数据 |
| 订单列表、详情刷新、重复提交、并发、截止/超时、收藏 | 真实客户端封装、事务夹具、OrderSync；修复软删除订单收藏和维护通知，查询/事务均重检 |
| 注册、用户资料、常用联系人/地址、后台用户/设置 | 原真实路由/表单/SDK 回归保留，未修改此前授权和默认资料方案 |
| 评价/信誉、申诉、邀请、校区客服 | 审查相关服务与页面、角色/范围/幂等回归；未确认需追加修改的问题 |
| 公告、通知、关于页、后台公告编辑 | 实际 callCloudData 吞错边界探测；修复读取失败误当空数据、草稿丢失和订阅隐藏页锁 |
| 后台订单/报表/运行监控、学校/校区配置、迁移/任务 | 源码和路由审查、已有自动化与 SDK 离线检查；不把本地模拟算作云端或真机验收 |
| 页面模板 | 微信真实编译器检查；新增八个状态覆盖跳阶段、旧数据和代买/代送角色；修复重复购买要求和代买订单信息不可见 |

- ISO 历史时间、负数字符串/非法日期以及无时间但有动作的情况均先补失败用例；当前进度 15/15 通过。新增 MAIL_MILESTONES 只包含五个字段的布尔值，避免裁剪私密历史时丢失动作存在证据；读取不改原始记录，也不捏造时间。
- 文档删除名单：FEEDBACK_VALIDATION.md、PUBLISH_PRESSURE.md、ARCHITECTURE_PROGRESS.md、deployment/ADDRESS_AUTOFILL.md、校园跑腿小程序安装部署使用手册.docx。有效内容已合并；其余维护文档不再把旧测试数量和旧包称作最新。7 份 Markdown 的 34 个本地链接均有效。
- 快照比较只把 733 份清单范围用于增量，图片/隐藏配置等未纳入原清单的文件不能误报为本轮新建。没有撤回此前工作区改动。

### 最终核验（2026-09-22）

592/592 自动化、363 JS/CJS/103 JSON/943 导入/126 云路由、200 实际页面状态、119 WXML/WXS/131 WXSS、组件样式及增量审查均通过。备份733个哈希复核一致。当前本地 worker 批次为 architecture-B8Pp0p，三包各122文件验证通过，uploaded=false；本地结果不等于云端上线或真机验收。
