# 本轮进度

## 2026-09-21

- 已读取既有页面、配置服务、订单校验、服务状态显示及相关测试。
- 已向用户说明后台已有总开关，本轮增加独立营业时间限制开关，默认关闭。
- 已确定四处新增选择器只移除 UI，底层组件和租户逻辑保留。
- 正在补充行为回归，再实施最小改动。
- 已补充 5 项行为回归：旧配置及关闭开关时全天发布、实际订单发布与接单和履约、无效值与管理员权限、后台页面到配置服务保存、校区间隔离。原有营业边界用例改为显式启用时间限制。
- 已确认云请求助手会自行 `ensureScope()`，管理员请求也保留停用校区的恢复上下文初始化；不依赖选择器组件挂载。
- 已运行修改前回归：63 项，58 通过，5 项新增用例按预期失败。已逐条核对为功能缺失造成的失败，而非测试加载错误。
- 已移除四处新增选择器挂载；新增后台“按营业时间限制发布与接单”开关，默认关闭，并贯通配置校验、权限、分组保存、前端状态及云端校验。
- 已增加深夜不限时间的实际 WXML 渲染场景，并保留显式开启限制后的休息状态场景。下一步运行修改后核验。
- 修改后全量回归 408/408 通过、无失败或跳过，日志 `.tmp/architecture-internal-test-tests.log`。
- 64 个页面、118 个模板的结构检查通过；346 个 JS/CJS、103 个 JSON、908 个相对导入、125 个云路由检查通过。
- 微信编译器通过：119 个 WXML/WXS 输入、131 个 WXSS 输入。正在补跑带实际渲染场景的页面检查，并刷新受源码改动影响的本地部署快照。
- 实际微信模板渲染 160 种页面状态通过，包含深夜默认不限时与启用后的休息状态；证据 `.tmp/architecture-internal-test-render.log`。
- 本地部署快照已刷新为 `.tmp/deploy/architecture-uAf27U/`；三包各 121 文件及 47 个候选索引材料核对通过，`uploaded=false`，未执行远程操作。
- 已同步 `ARCHITECTURE_PROGRESS.md`、`ADMIN_CONSOLE.md` 与部署手册，明确当前内测隐藏 UI、时间开关默认关闭，以及多校开放前需要恢复选择入口。
- 最终差异已复核，`git -c core.autocrlf=false diff --check` 退出码 0；页面模板中没有 `campus-selector` 挂载。
- 本轮本地任务完成：两项用户要求已落实、回归及实际编译/渲染通过、文档已更新。无需再次运行与本轮无关的云端 SDK 或容量测试；真实部署仍未执行，须同步更新 `mcloud` 与小程序后在内测环境生效。
- 本轮未进行远程部署或修改用户原有 `cloudfunctions/mcloud/config/config.js`。

## 全局默认标题保留

- 用户要求默认标题为 `GXNU随手取`，不要改动。已将 `miniprogram/app.json` 对应值从通用名称恢复，并同步进度书与保留约束。
- 本次仅调整标题配置，不新增单元测试；JSON 解析与字段精确值核验通过，Git 差异确认标题已恢复为原值，`git diff --check` 通过。

## 微信登录反馈排查

- 已读取 systematic-debugging 流程，核对“我的”页、注册页、公共 PassportBiz、云端微信身份与订单用户校验。
- 已询问用户“入口消失”与“未登录也能下单”两种场景，继续开展不依赖回答的代码与回归核验。
- 静态证据显示入口合并在顶部资料卡，已注册用户存在静默登录；登录校验代码未被上轮移除。尚未定位用户实际症状，不做推测性功能修改。

## 微信手机号登录与强制补全资料

- 用户已澄清点击后只有注册，要求微信绑定手机号登录，再强制补全原注册资料；连续“继续”保持授权。
- 已完成根因追踪、读取相关技能与测试适配器，开始补充真实业务回归后实施。
- 前期工具错误已定位：Git 不支持把 Windows NUL 用作 exclude 文件；改回普通只读 status。PowerShell 不支持 Bash 路径花括号，已改显式路径。个人资料入口为 `my_personal.js`。

## 微信登录实现与本地回归收尾

- 已接入 `passport/wechat_login`：一次性手机号 code 由云端 OpenAPI 兑换，使用可信微信身份与 AppID 水印，按学校在事务中绑定已验证号码。
- 注册及业务权限同时检查已验证手机号与完整资料；注册后仍遵守审核／停用状态。客户端提供微信手机号登录、只读号码及重新授权更换、可编辑昵称与头像、强制资料补全。
- 首轮全量回归为 437 项、432 通过、5 失败。失败集中在旧测试没有模拟授权、地址夹具覆盖必填资料、个人页断言依赖旧模板字符串。
- 已修复以上测试：管理请求链先授权再完成资料，地址变更保留现有个人字段，入口断言覆盖游客／旧账号／未验证／未完成／完整账号的行为。
- 续作定向回归命令 `node --test scripts/tests/admin-user-request-flow.test.js scripts/tests/delivery-address-filter.test.js scripts/tests/user-reported-pages.test.js`：36/36 通过。
- 独立审查正在验证注册页异步恢复及竞态；下一步补页面状态渲染、SDK 离线接口核验、全量回归与部署说明。
- 官方文档网络升级被自动审批服务拒绝（404，unsupported codex-auto-review model）；开发工具 CLI 当前受沙箱用户目录写权限限制。继续完成离线验证，不绕过限制，不将本地通过表述为真实微信授权成功。

## 微信登录本地收尾结果

- 已使用 receiving-code-review 处理独立审查的两项恢复问题。新增 4 个用例使用真实 PassportBiz；红跑 29/33，分别修复完整资料恢复时先同步会话、授权开始后忽略旧成功／失败读取，绿跑连同客户端测试 43/43；审查者复核通过。
- 完整回归 `npm test --prefix cloudfunctions/mcloud`：441/441 通过、0 跳过，约 7.00 秒；证据 `.tmp/wechat-login-tests.log`。
- 实际页面结构／渲染：64 页面、118 模板，177 种状态（新增 17 种微信登录／必填资料状态）；119 WXML/WXS、131 WXSS 编译通过。
- `check-wechat-login-sdk.cjs`：已安装 SDK 2.6.3 的手机号接口、一次性 code 序列化、phoneInfo 转换、失效 code 拒绝通过，2 次请求均本地拦截，无网络。源码检查 351 JS/CJS、103 JSON、915 相对导入、126 云路由通过。
- 已生成 `.tmp/wechat-login-preview.html`；浏览器读取本地验证说明时再次收到自动审批 404，未继续或改通道绕过，因此窄屏浏览器布局检查不计入通过项。
- 新本地部署批次 `.tmp/deploy/architecture-pYLv0M/` 的 3 包各 122 文件哈希、47 候选索引及触发器/权限材料通过；`uploaded=false`。旧批次已在进度书标为过期。
- 已新增 `deployment/WECHAT_LOGIN.md`，同步 README、架构进度书、多校部署手册；明确真实手机号服务资格／额度、隐私设置、原生授权、云部署均仍需实际环境验证。
- 最终 `git -c core.autocrlf=false diff --check` 通过；AppID、全局 GXNU随手取 标题、隐藏选择器及说明内容核验通过。用户原有 config.js 未修改。
- 所有本地材料完成后申请 `cli.bat islogin --project B:/zhumian/zm/kaidi` 的目录访问升级，用于确认开发工具登录状态；自动审批服务再次以 404（不支持 codex-auto-review）拒绝，命令未执行。不绕过限制，真实联调待审批服务及权限恢复。

## 真机点击手机号登录只转圈：续查

- 已读取当前登录页、公共登录模块、云包装器、云端兑换与路由日志；项目没有主动调用反馈中的后台预拉取接口。
- 已确认原生失败被统一提示掩盖、SDK 失败在 `hint:false` 时缺少诊断、云端兑换 catch 丢弃错误码。未确认用户是否看到授权弹窗，亦未确认用户是否已部署最新云函数；已发出两个简短异步问题。
- 沿用已授权的局部登录排错范围，先补充原生取消／隐私／权限／缺少 code、云环境／函数缺失／超时、页面可见反馈与安全日志的回归用例，再执行红跑。真实问题尚未宣布解决。
- 红跑 69 项中 58 通过、11 个新增用例因错误提示丢失／没有诊断失败；证据 `.tmp/wechat-login-diagnostics-red.log`。
- 已在公共手机号登录链路区分原生取消、隐私、能力不可用、缺少 code、云环境／云函数不存在及超时。注册页失败时同时显示 toast 和内联文字，finally 释放按钮；点击埋点不设置 loading/disabled，不干扰微信原生弹窗。
- 新日志统一前缀 `[wechat-phone-login]`，只含固定阶段、固定分类及受限数值错误码。云端 OpenAPI catch 保留数值错误码；一次性 code 仍只兑换一次，不自动重试。没有改变隐私配置或认证／资料门槛。
- 定向绿跑 69/69、全套 452/452（0 失败、0 跳过，约 7.89 秒）通过。177 种实际 WXML 状态、119 WXML/WXS 与 131 WXSS 编译通过；源码检查 351 JS/CJS、103 JSON、915 相对导入、126 路由通过。
- 原审查代理按 requesting-code-review 对本次增量做只读复核，未发现需修复项；独立重跑 69/69 通过。没有扩大到此前全部未提交架构改动。
- 已同步真机分阶段排查说明、README 和进度书；新生成本地批次 `.tmp/deploy/architecture-Dp6af9/`，尚待最终快照校验，`uploaded=false`。用户授权弹窗／部署状态问题仍无答复，真机根因保持未确认。
- 最终快照检查通过：三个包各 122 个文件哈希、47 候选索引及权限／触发器材料通过。最终 `git -c core.autocrlf=false diff --check` 通过；未重新尝试受限联调工具，未部署云端或修改真实账号。下一步需要用户这次真机的 `[wechat-phone-login]` 回调及是否出现原生弹窗／是否已部署最新 mcloud 的观察结果。

## 手填注册续作开始（2026-09-22）

- 延续用户授权：手机号／微信能力不可用时改为手填注册，并放行资料完整的内测账号进入业务功能。
- 已核查前后端门槛与页面缓存逻辑，准备按新的服务端开关补充失败回归后实施。保留未提交工作、可信身份和审核状态。
- 已保存 24 个变更前文件至 `.tmp/manual-registration-before/`，供本轮增量复核。
- 新增服务端创建／发布、身份与手机号归属、审核、竞态及严格模式恢复回归；客户端覆盖手动入口、102 自动回退、头像选择、缓存策略、资料导航及回包丢失恢复。首轮 96 项中 68 通过、28 按预期失败，证据 `.tmp/manual-registration-red.log`；开始实现。
- 已完成前后端开关与手填流程；服务／登录 52 项、资料页面 44 项通过。实际模板先复现缺少手填入口，接入后 186 状态、119 WXML/WXS、131 WXSS 通过。
- 全量 491/491 通过，0 失败、0 跳过；源文件检查 352 JS/CJS、103 JSON、924 相对导入、126 路由。依照 requesting-code-review 已派只读审查，仅评判本轮增量。
- 文档补丁最后一处上下文因目标句属于整段未匹配，整文件修改未应用；已按完整原文更正，不涉及产品源码。
- 追加图片选择能力回归：存在 chooseMedia 但 canIUse 明确不支持时须回退 chooseImage；先复现失败后修复。现代 picker 和取消保留图片均通过。最终全量 492/492，源检查及全局标题／选择器保留项通过。
- 已更新登录、README、多校运行说明与进度书；生成 26 状态的本地页面预览。新部署批次 `.tmp/deploy/architecture-Wo6S9i/` 的三个包各 122 文件哈希、47 候选索引、权限与触发器材料通过，uploaded=false。独立审查最后的实际路由和异步探测仍在进行。
- 审查确认一个 P2 时序问题：手机号权限错误早于 operations/config 回包时不自动回退。补充允许／严格两项用例，允许模式先失败；保存原生能力错误并在统一策略应用时恢复手填，99/99 定向通过。
- 修复后全量 494/494，0 失败／跳过，约 7.34 秒；186 状态渲染、119 WXML/WXS、131 WXSS 和源检查再次通过。此次仅前端改动，已校验的后端部署快照仍对应最终源码。
- 最后审查发现 P3 提示残留：102 后再成功授权，真实用户回包携带 allowManualRegistration=true 时会重新写入手填提示。强化既有成功回归并确认在 loginError 断言失败，证据 `.tmp/manual-registration-success-message-red.log`；成功分支已同步清空 loginError，等待本轮验证与复核。
- 提示修复后最终全量仍为 494/494，0 失败／跳过，约 6.89 秒；352 JS/CJS、103 JSON、924 相对导入、126 路由源码检查通过。该修复仅清空客户端旧错误，没有改变模板或后端；此前实际模板编译与 architecture-Wo6S9i 部署材料核验仍适用。登录说明已补充成功提示回归证据。
- 独立增量审查最终通过：逐文件对照 24 份本轮基线，P2 延迟配置和 P3 成功后旧提示均已复核，无未解决的可行动问题。三份定向回归 99/99、最后提示清理强化用例 1/1 通过；真实应用路由配合内存库验证手填账号可接单、表单身份伪造无效、缺少 OPENID 被拒以及关闭开关恢复验证要求，手填路径未调用手机号 OpenAPI。
- 当前手填注册范围全部本地完成，已同步计划与文档；管理员凭账号密码的既有内测模式继续保留。未部署、未更改真实账号；真实微信平台权限、原生界面与云端性能仍不在本地验收结论内。

## 手机号 errno 102 与后台内测登录

### 最新续作：永久去除后台学校／校区授权

- 用户明确放弃学校／校区管理授权门槛。已定位唯一报错抛出处 tenant_context.assertAdmin；登录、会话、订单事务、设置和用户管理均存在相关调用，客户端也存在校级入口门槛。
- 既有默认内测开关仍保留严格分支；本轮直接删除授权调用和判断，环境变量无法恢复学校／校区限制。已保存 18 个文件的增量基线。实际云端版本尚未核实，之前未部署的事实不变。
- 调整后台登录、真实工作台／用户详情路由、事务和设置回归；41 项红跑中 34 通过、7 项按预期因学校／校区／校级授权或前端入口失败（`.tmp/admin-scope-removal-red.log`）。随后删除全部 assertAdmin/assertSchoolAdmin 及其实现，前端用户入口不再依赖 scopes 或 credentialsOnly，审核开关仅按超级管理员角色显示。
- 定向 41/41、全量 497/497 通过，0 失败／跳过，全量约 8.04 秒。186 状态渲染、119 WXML/WXS、131 WXSS、352 JS/CJS、103 JSON、916 相对导入与 126 路由检查通过。生产源码已无两条学校授权报错及相关校验方法。
- 新本地快照 `.tmp/deploy/architecture-XAyZU4/` 三个包各 122 文件哈希、47 候选索引、权限和触发器核验通过，uploaded=false。README、后台说明、微信登录、多校部署和进度书已同步永久移除后的规则与实际上传步骤。
- 原后台审查代理本轮因服务繁忙未能启动，不算审查通过；已交由现有手填注册审查代理只读复核此次 18 份文件增量，等待结论。
- 独立审查已确认 41/41 回归及生产源码无学校授权残留；发现 README 一处旧补授权说明与一处令牌失效描述范围过宽，均按实际代码修正。最终写入路由和负向数据范围探测仍在进行，产品源码未追加修改。
- 独立增量审查最终通过：18 份基线逐文件复核，41/41 回归通过；内存探测确认 true/false 两种配置下订单操作、用户状态修改和超级管理员设置不再要求学校／校区授权。跨范围记录、普通管理员高级配置与非平台目录访问仍被拒绝，令牌／身份／有效期验证有效。两处文档已复核，无未解决的可行动问题。
- 本轮本地工作全部完成。最终 497/497、编译及 architecture-XAyZU4 仍对应最终代码；未上传云端或修改真实账号。后台说明已明确 mcloud 的“上传并部署：云端安装依赖”步骤，不能仅重新编译小程序。

- 用户已补充原生 `unavailable / 102` 及后台校区权限错误。已将问题缩小至微信手机号能力权限与新后台授权门槛。
- 对照 Git 基线确认旧登录不强制取得微信验证手机号；准备改进权限错误说明。
- 后台按用户要求采用临时凭账号密码模式，开始补充真实服务登录、会话、事务和恢复严格模式的回归。
- planning 初次补丁因 progress 上下文写错未应用，已检查后修正；未影响产品源码。
- 红跑 32 项中 17 通过、15 项按预期失败：后台缺 scopes 被拒、停用状态／限流／短账号、无效登录回包，以及手机号权限分类；证据 `.tmp/admin-login-red.log`。
- 完成服务端临时登录开关，登录／会话／写事务统一，角色与数据库隔离保留。仅给现有账号签发会话，不修改账号角色、状态或授权。客户端接受服务端临时模式并阻止无效回包导航。
- 定向 70/70 通过；全量 466/466，0 失败、0 跳过，约 7.04 秒。源码检查 352 JS/CJS、103 JSON、922 相对导入、126 路由；177 种 WXML 状态、119 WXML/WXS 和 131 WXSS 编译通过。
- 已同步手机号 102 原生权限原因与旧版身份登录差异、后台开关及恢复方式。仅本地核验，尚未上传 mcloud；真实微信能力开通仍需公众平台操作。
- 独立审查代理两次因服务高峰启动失败，无审查结果可引用；不将其算作通过。
- 已为最后一次独立审查改用可用模型，仍限定这次增量与只读检查；没有重复实现任务。
- 新部署批次 `.tmp/deploy/architecture-g0W2OF/` 三包各 122 个文件哈希、47 候选索引及触发器/权限材料通过。文档已标记旧快照过期，API 修复仍需单独上传 `mcloud` 并重新编译。
- 全局标题保持 GXNU随手取，页面无 campus-selector 挂载，`git -c core.autocrlf=false diff --check` 通过。选择器无匹配的 rg 退出1为预期，并非检查失败。
- 独立审查最终通过：逐文件对照 12 份本轮变更前镜像，无可行动缺陷；相关 58/58 回归通过。追加内存探测验证实际工作台、客服回复事务，以及同一内测 token 在恢复严格模式后重新受状态／校区／校级权限约束；角色和平台身份门槛有效。
- 审查未覆盖此前全部架构改动、真实微信平台权限或真实云部署。用户列表追加探测仅覆盖鉴权入口，不把夹具未实现的描述格式化计作验收。无新增产品修改，466/466 全量及当前部署快照仍对应最终源码。
- 2026-09-22 新反馈：注册和后台设置均显示笼统文字违规。已定位共用审核的异常误分类和过宽审核输入，开始调查实际字段与启用审核的路由回归；本阶段尚未修改产品代码。
- 已保存 11 份本轮基线。文字审核 31 项回归已完成有效红跑（2 通过、29 预期失败）；下一步实现服务故障分类、资料字段选择、配置规范化及未改文字比较。
- 文字审核修复实现完成：注册 scene=1 按真实字段检查；配置先规范化并只审核变更文字，已有批量提交保持一次调用。接口异常按权限／身份／繁忙／超时／异常回包区分，实际拒绝说明具体字段。
- 回归夹具修正为应用实际 `crun^^^` 身份，并在请求 JSON 所在 realm 执行同一份真实 data_check；未跳过对象校验。使用本轮源文件镜像重新确认 31 项初始红跑（29 失败），不为红跑覆盖工作目录文件。
- Unicode 分段、旧补充字段与布尔值覆盖、原型同名标记、相同显示标签覆盖等边界均补回归先失败再修复。最终 97/97 定向、535/535 全量、SDK 2.6.3 六次离线拦截、354 JS/CJS／103 JSON／927 导入／126 路由通过；186 模板状态、119 WXML/WXS、131 WXSS 通过。
- 新本地部署批次 architecture-QAsZkn 三包各 122 文件校验通过，uploaded=false。已新增 CONTENT_AUDIT.md 并同步相关说明；独立增量审查完成，85/85 相关回归、16 项真实路由失败探测及 SDK 离线核验通过，无未解决的可行动问题。
- 独立复核覆盖未修改说明、自定义标题、审核期间状态变化以及最终五份生产文件，确认审核失败时原资料与配置不变。审查后未再改生产代码；部署说明中的测试数、快照名称与未上传状态已核对，diff --check 通过。本轮本地修复与验收收尾，未上传云函数、未修改真实账号、未执行真机验收。
- 用户追加手填注册“微信文字审核服务暂不可用”截图。已请求本次 `[text-audit]` 的 reason／errCode 以便核对线上，同时排查本地真实调用链。
- 离线使用真实 cloud_base 与 SDK 2.6.3 复现：原 openapi 为 function，包装后为 object／Promise，security 不存在，传输层收到错误 API `bind`。此前 SDK 检查直接调用原始 SDK，实际路由夹具又替换 cloud_base，因而遗漏了二者组合。准备只修复这一包装错误，补经过真实边界的测试。
- 保存 10 份增量基线至 `.tmp/cloud-sdk-wrapper-before/`。三个 SDK 脚本改为实际生产封装，新增三组全量集成测试；红跑 0/3 通过，审核准确重现用户截图，三类调用均误发 bind。生产只增加 OpenAPI namespace 原样返回的两行，随后 3/3 通过。
- 全量 `npm.cmd test --prefix cloudfunctions/mcloud` 538/538 通过，约 7.23 秒；源码 355 JS/CJS、103 JSON、930 导入、126 路由通过。审核脚本含 12 次离线请求及实际注册路由通过／拒绝／权限错误不落库，手机号 2 次、数据库事务与通知组合也通过。客户端未改，不重复模板编译。
- 新本地部署批次 architecture-LL62UW 三包各 122 文件哈希／触发器／权限材料通过，uploaded=false；已同步部署说明，明确须上传最新 cloud_base.js。独立只读审查已发起，待最终结果。
- 独立增量复核完成，无可行动发现：10 份基线哈希一致，生产仅两行增量；在内存加载基线重现 bind 请求／Promise／缺失 security，现版读取 namespace 不发请求。三组集成 3/3 通过，附加 HTTP/HTTPS/TCP/TLS/fetch 拦截下连接尝试 0；namespace 配置、图片审核入口、错误传播和普通方法绑定探测通过。
- 复核未覆盖真实微信权限／额度／云端版本／真机身份及历史完整架构；本地材料不计作上线完成。主代理完成部署说明同步与验收记录收尾，本轮仍未上传、未修改真实账号。
- 2026-09-22 追加常用地址／粘贴日志续作：已读地址保存及默认映射、发布页和实际表单组件，定位数组下标错配与提交后遗留的地址手填标记。保存 24 份本轮增量基线至 .tmp/address-autofill-before/；准备真实组件与页面生命周期回归，尚未改产品代码。
- 常用地址与表单本轮：14 项真实组件／地址保存／连续下单回归通过（首批 12 项红跑 7 失败），当前全量 554/554；192 个页面状态、119 WXML/WXS 与 131 WXSS 编译通过。已修复 mark 同步、父页快照重置、手填标记生命周期和资料请求顺序；已合并重复默认 slot，换用 getWindowInfo，并将日志涉及组件的标签／属性选择器改为类选择器。27 份增量基线；继续核验组件模式样式和独立增量审查。
- 最终组件核验准确复现七个基线警告及原日志位置，现版诊断全部为空；检查器集成后重跑 192 页面状态、119 WXML/WXS、131 WXSS 与七个组件样式全部通过。源码最终 359 JS/CJS／103 JSON／932 相对导入／126 路由通过，diff --check 无输出。
- 独立增量审查完成：27 份基线哈希一致，109/109 相关测试及三种服务、即时／延迟属性传递、各连续三次发布共 18 次提交通过；旧版重置越界可复现，现版正常，必填与手机号校验有效。未发现可行动问题。
- 已补 deployment/ADDRESS_AUTOFILL.md，说明默认地址、当前草稿、下一单和编辑订单的行为，以及本地证据与重新编译／预览步骤。本轮前端修复已完成本地核验；未上传云函数、未修改真实账号、未执行真机验收。

## 2026-09-22 全功能排查续作

- 已将五阶段真实进度用于用户详情、接单卡片和后台详情；管理员直接完结不会虚构接单／取件／送达。初始回归 7 项中 6 项失败，相关 49/49 在修复后通过。
- 独立复核发现历史事件被 DTO 裁剪、缺失旧记录显示为待执行，以及字符串零被 controller 转成 1970 时间。新增 5 项真实 controller/service 回归先全部失败；现仅在输出 DTO 补有效事件时间、保留隐私裁剪、不改持久化原记录，并过滤无效时间；12/12 通过。已请求最终只读复核。
- 服务端审查修复软删除订单收藏与维护通知：收藏 3 项、维护 6 项先失败，相关 74/74 通过；查询和事务重检均排除删除订单。
- 客户端审查使用实际云封装复现吞错，修复公告／反馈／关于／后台公告页刷新后内容或草稿丢失及隐藏页面订阅锁。14 项回归先 8 失败后全通过，相关 132/132 通过。
- 删除 5 份过时重复文档并将有效内容合并到维护文档；基线 ZIP 保留原文件。剩余旧测试数和旧快照说明继续清理。
- 最终全量测试、源码检查、WXML/WXSS 编译及新的 worker 包校验尚未执行；不把上述定向结果表述为最终全量或云端验收。
- 最终复核中补 ISO 历史、畸形日期、无时间动作三项行为，均先失败后修复。当前进度 15/15；安全摘要只投影动作存在布尔值和有效时间，不含私密历史、不改持久化记录。
- 首轮全量 589/589、源码 363 JS/CJS/103 JSON/943 导入/126 路由通过。模板旧夹具仍断言“配送中”已按五真实动作更新；新增逐节点、连线和内容数量检查，真实复现代买两块购买要求并修复，200 状态/119 WXML/WXS/131 WXSS/7 组件样式通过。
- 客户端/服务端交叉只读复核无可行动问题；进度新增安全摘要待最终复核。文档清理完成，7 份维护文档与34个本地链接校验通过；下一步最终全量、源码和当前 worker 打包。
- 本轮最终验收：592/592 全量通过（.tmp/function-audit-full.log）；363 JS/CJS、103 JSON、943 相对导入、126 路由通过；200 页面状态、119 WXML/WXS、131 WXSS、7 个组件样式通过。源码在通过后未再改行为；README 和旧测试文件仅统一混合换行，确认无语义变化，diff --check 最终无输出。
- 三方独立/交叉审查全部关闭可行动问题。进度复核额外三服务/四角色24场景与34状态边界断言通过；安全标记只有五个布尔值，原对象不变。
- 本地新部署批次 architecture-B8Pp0p，ordersWorker/notificationsWorker/tenantMigrator 各122文件哈希、触发器、权限及47候选索引材料通过，uploaded=false。API仍须上传当前 cloudfunctions/mcloud；本轮未连接云端、未修改真实数据、未执行真机验收。
- 733 份起点 ZIP 与 SHA-256 清单全部复核一致。增量36份修改（含单独核对的.gitignore）、4份新测试、5份删除文档；未在备份清单中的旧图片/隐藏配置不误报为新增。7份维护文档34个本地链接有效。
