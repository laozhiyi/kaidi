# 申诉链路与并发验证

用户提交调用 `feedback/insert`，管理员通过 `admin/feedback_list` 和 `admin/feedback_detail` 读取同一张 `bx_feedback` 集合。处理结果由 `admin/feedback_reply` 写回，用户从“我的申诉”详情查看；回复同时生成一条站内通知。这里没有额外的人工转发步骤。

## 本次处理

- 申诉编号由项目、用户和请求标识确定。重复请求返回原编号；改变内容却重复使用同一标识会被拒绝。
- 已提交请求在内容审核之前核对原始指纹。响应丢失后的重试不再重复审核、归档图片，也不重复占用提交次数。
- 新申诉与用户提交次数在同一个数据库事务中写入，失败一起回滚。不同用户使用不同的限流记录，没有所有用户共用的可写计数器。
- 网络超时、断网等明确的临时故障最多自动重试两次，使用相同请求标识，并逐次延长等待。业务校验失败不会自动重试；最终失败后保留本页内容供手动重试。
- 图片审核每次最多并行两张，全部成功后才创建申诉。管理员回复继续使用版本校验，冲突时提示刷新，避免覆盖其他管理员的处理结果。
- 原有限制仍生效：每个用户每小时最多新提交 5 条，每分钟最多发起 10 次新提交的审核；重复读取已成功的请求不占用这些次数。

## 已完成的本地验证

```powershell
node --test scripts/tests/feedback-concurrency.test.js scripts/tests/feedback-network-retry.test.js
```

测试使用允许事务并行执行、按文档检测冲突的数据库模拟器，覆盖：100 个不同用户同时提交、30 个相同请求同时写入、提交成功但响应丢失后的重试、写入中断回滚、审核中断、限流隔离、管理员同时回复，以及用户读取处理结果和通知。模拟器验证数据一致性和请求行为，不代表云服务的实际吞吐量或延迟。

## 部署和云环境验收

需要重新部署 `cloudfunctions/mcloud`，并编译上传小程序。检查云数据库的复合索引是否覆盖实际查询；本项目集合前缀为 `bx_`，建议至少包括：

| 集合 | 查询用途 | 索引字段顺序 |
| --- | --- | --- |
| `bx_feedback` | 我的申诉 | `_pid` 升序、`FB_USER_ID` 升序、`FB_ADD_TIME` 降序、`_id` 降序 |
| `bx_feedback` | 管理员全部申诉 | `_pid` 升序、`FB_ADD_TIME` 降序、`_id` 降序 |
| `bx_feedback` | 管理员按状态查询 | `_pid` 升序、`FB_STATUS` 升序、`FB_ADD_TIME` 降序、`_id` 降序 |
| `bx_order_review` | 我对别人的评价 | `_pid` 升序、`REVIEW_FROM_USER_ID` 升序、`REVIEW_ADD_TIME` 降序、`_id` 降序 |
| `bx_order_review` | 别人对我的评价 | `_pid` 升序、`REVIEW_TO_USER_ID` 升序、`REVIEW_ADD_TIME` 降序、`_id` 降序 |

管理员同时使用类型筛选时，按云数据库索引建议增加 `FB_TYPE` 对应的组合索引。`_id` 的唯一索引由数据库提供，不需要单独给请求指纹建唯一索引。

上线前，在测试云环境用独立测试账号分别测纯文字和六张凭证图片的 20、50、100 并发请求，核对成功响应数、实际记录数、管理员可见数量和回复结果，并记录超时率与 P95 延迟。结合结果配置云函数并发额度、内存、超时时间、数据库容量和微信内容审核接口配额。本次没有调用线上接口、修改云资源或进行真实云压测，不能据此保证线上在任意并发量下都不失败。
