<view wx:if="{{isLoad}}" class="main padding-project">
  <include src="personal_form.wxml" />
  <button bindtap="bindSubmitTap" disabled="{{saving}}" class="btn-base btn-project shadow">修改资料</button><view wx:if="{{saveError}}" class="profile-required-hint">{{saveError}}</view>
</view>
